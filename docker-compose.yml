version: "3"

services:
  php:
    build:
      context: ./docker
    restart: always
    volumes:
    - ./app:/var/www/html
    - ./docker/php.ini:/usr/local/etc/php/php.ini
    ports:
        - "10080:80"
        - "2222:22"
    tty: true
    stdin_open: true
